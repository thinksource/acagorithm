import unittest
from ACalgorithm import ACBuild

class AlgorithmTest(unittest.TestCase):

    def setUp(self):
        self.text = "ushers"
        self.param = ["he", "she", "his", "hers"]
        self.ac = ACBuild(text, Pattens)

    def test_build(self):
        self.ac.buildGotoTree()
        self.ac.